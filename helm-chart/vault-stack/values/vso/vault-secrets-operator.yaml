# Vault Secrets Operator Configuration
# Official HashiCorp Helm chart values

# Controller configuration
controller:
  # Manager configuration
  manager:
    image:
      repository: hashicorp/vault-secrets-operator
      tag: 0.9.0
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 128Mi
    # Client cache configuration for Vault
    clientCache:
      persistenceModel: "direct-unencrypted"
      storageEncryption: false

  # Metrics service configuration
  metrics:
    service:
      type: ClusterIP
      port: 8443

# Default Vault connection configuration
defaultVaultConnection:
  enabled: true
  address: "http://vault-stack:8200"
  skipTLSVerify: true

# Default authentication method
defaultAuthMethod:
  enabled: true
  method: "kubernetes"
  mount: "kubernetes"
  kubernetes:
    role: "vso-role"
    serviceAccount: "default"
